trial = loadtrial("inputs/input.xml")
net = loadnetwork("networks/network.xml")
sim = initsimulation(net)
sim = linktrial(trial, sim, "CN")
sim=run(sim,"output",2)
# graphinputs(trial, "inputs.eps")
# graphnetwork(sim, "network.ps")
graphtrial_voltage(sim, 0, 0, "trial.eps")
quit()
