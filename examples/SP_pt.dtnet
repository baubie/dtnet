###############################
# Coincidence Detection Model #
# Written by Brandon Aubie    #
###############################

T = 100
sim = simulation("networks/SP_pt.xml", "inputs/pt_durations.xml")
results = run(sim,"/1/sp_pt.sim",15,5,false)
#results = load("/1/sp_pt.sim")
print(results)
graph_height = 10
graph_width = 10
graphspiketrains(results, "DTN", "sp_pairedtone.pdf")
rw = constrain(results, "trial.main.delay", 60)
quit()
