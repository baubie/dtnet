###############################
# Coincidence Detection Model #
# Written by Brandon Aubie    #
###############################

T = 100
sim = simulation("networks/BP_pt.xml", "inputs/pt_BPdurations.xml")
results = run(sim,"/1/bp_pt.sim",10,5,false)
#results = load("/1/bp_pt.sim")
print(results)
graph_height = 10
graph_width = 10
graphspiketrains(results, "DTN", "pairedtone.eps")
quit()
