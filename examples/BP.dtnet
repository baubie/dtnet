###############################
# Coincidence Detection Model #
# Written by Brandon Aubie    #
###############################

sim = simulation("networks/BP.xml", "inputs/durations.xml")
results = run(sim,"bp.sim",15)
#results = load("bp.sim")
durs = constrain(results,"trial.main.duration",5)
graph_height = 10
graph_width = 10
graphspiketrains(durs, "DTN", "spikes.eps")
quit()
